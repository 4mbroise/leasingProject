Il s'agit que du backend, Il faut utiliser MySQL avec les login par défaut avec une base de donnée nommée "leasing" et y créer les tables en se servant du script à la racine du projet
Il me reste à faire la totalité du front, terminer le back des véhicules et commencer le back des locations

Concernnant les librairies utilisées, il me semble que j'utilise juste Jackson pour parse mes objets en Json/XML, c'est compris dans le projet maven donc ça ne pose pas de problème, rien à ajouter au projet manuelement.

C'est que le backend, donc on y trouve que certains endpoints, je détaillerais + quand ce sera bouclé.